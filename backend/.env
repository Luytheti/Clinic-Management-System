# Environment variables for the backend
# gvenzl/oracle-xe:21-slim uses XEPDB1 as the pluggable DB service name
# Replace 'oracle' with whatever password you set via ORACLE_PASSWORD when you first ran the container
DATABASE_URL=oracle+oracledb://system:oracle@localhost:1521/?service_name=XE
